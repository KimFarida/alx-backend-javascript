import ClassRoom from "./0-classroom.js";

export default function initializeRooms() {
  const a = new Classroom(19);
  const b = new Classroom(20);
  const c = new Classroom(34);
  const sizeList = [a, b, c];
  return (sizeList)
}
